import type { Role } from "@/types";

export const APP_NAME = "Inkspire";
export const POSTS_PER_PAGE = 6;
export const AVAILABLE_ROLES: Role[] = ["viewer", "author", "admin"];
export const IMAGE_BUCKET = "post-images";
